/* let money = 500;
money = "";
let kony = false;
money = 55;
 */

let money: number = 5;
let honey: string = "O my sweet hear Honey dew dew peu peu";

console.log(honey);
let funny: boolean = true;

function add(first: number, second: number): number {
  const result = first + second;
  return result;
}

const result: number = add(45, 87);

function doubleConsole(number: number): void {
  console.log(2 * number);
}

//multi purpose
// function add(first: number | string, second: number | string): number |string{
//   const result: number | string = first + second;
//   return result;
// }

// const output: number | string= add(45, 87);
// const fullName = add("Adam", "Sandlar")
